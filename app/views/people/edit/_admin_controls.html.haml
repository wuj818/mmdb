.admin-controls
  = link_to @person, :class => 'btn', :rel => :nofollow do
    %i.icon-arrow-left
    Back

  = link_to @person, :class => 'btn btn-danger', :rel => :nofollow,
    :confirm => CONFIRM, :method => :delete do
    %i.icon-trash.icon-white
    Delete
