.admin-controls
  = link_to @person, :class => 'btn' do
    %i.icon-arrow-left
    Back

  = link_to @person, :confirm => CONFIRM, :method => :delete, :class => 'btn btn-danger' do
    %i.icon-trash.icon-white
    Delete
