.page-header
  %h1 Add New Movie

= form_tag scrape_info_path, :class => 'form-inline' do
  = label_tag :imdb_url, 'IMDB'
  = text_field_tag :imdb_url, nil, :autofocus => true

  = submit_tag 'Submit',
    :class => 'btn loading',
    :data => { :'loading-text' => 'Submitting...' }
