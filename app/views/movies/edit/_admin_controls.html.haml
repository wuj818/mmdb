.admin-controls
  = link_to @movie, class: 'btn btn-secondary btn-lg mr-1', rel: 'nofollow' do
    = icon 'arrow-left'

  = link_to @movie, class: 'btn btn-danger btn-lg mr-1', rel: 'nofollow', method: 'delete', data: { confirm: t(:confirm) } do
    = icon 'trash-o'
