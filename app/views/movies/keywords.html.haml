= page_header do
  %h1
    = link_to 'Movies', movies_path
    %span.divider /
    = link_to @movie.full_title, @movie
    %span.divider /
    Keywords

- smart_cache do
  #full-keywords
    %ul.list-unstyled
      - @movie.sorted_keywords.each do |keyword|
        %li
          - path = "/keywords/#{CGI::escape keyword.name}"
          = link_to keyword.name, path, class: 'badge badge-primary'
